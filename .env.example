# The service is single-user and binds to localhost by default.
# Optional: set PROOFFLOW_DATA_DIR to move local private storage.
# No OpenAI subscription or API key is required for calculation or demonstration.
